paths:
  /courts:
    get:
      tags:
        - Court
      summary: Gets list of predefine system courts
      produces:
        - application/json
      responses:
        200:
          description: Successfully fetched courts
          schema:
            type: array
            items:
              $ref: "#/definitions/Court"
        5XX:
          description: Unexpected error.
    # post:
    #   tags:
    #     - Court
    #   summary: Add a new playable court
    #   produces:
    #     - application/json
    #   parameters:
    #     - in: body
    #       name: court
    #       schema:
    #         $ref: "#/definitions/Court"
    #       required: true
    #       example: { userId: 123 }
    #   responses:
    #     201:
    #       description: Created
    #       schema:
    #         type: array
    #         items:
    #           $ref: "#/definitions/Court"
    #     5XX:
    #       description: Unexpected error.

definitions:
  Court:
    type: object
    properties:
      id:
        type: string
      coordinates:
        type: object
        properties:
          lat:
            type: number
          lng:
            type: number
      country:
        type: string
      state:
        type: string
      city:
        type: string
      district:
        type: string
      zip:
        type: string
      schedule:
        type: object
